;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-lambda-reader.ss" "lang")((modname 425ex) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; Exercise 425
;; ------------
;; Articulate purpose statements for smallers and largers in figure 149.
;; -----------------------------------------------------------------------------

; [List-of Number] Number -> [List-of Number]
; numbers in alon larger than n
(define (largers alon n)
  '())
 
; [List-of Number] Number -> [List-of Number]
; numbers in alon smaller than n
(define (smallers alon n)
  '())